# autovest — 원클릭 실행
#   cp .env.example .env   # 토스/키움 키 입력
#   docker compose up -d   # → http://localhost:8080 (서버 자신만 접근)
# 외부에서 보려면 SSH 터널 또는 Tailscale 사용 (DEPLOY.md 참고). 절대 0.0.0.0 으로 열지 말 것.
services:
  backend:
    build: ./backend
    env_file: .env          # 토스 키를 환경변수로 주입 (이미지에 굽지 않음)
    environment:
      - TOSSAPI_DATA_DIR=/appdata
      - TZ=Asia/Seoul
    volumes:
      - bot_data:/appdata    # 봇 설정·체결기록 영속화 (재배포해도 유지)
    restart: unless-stopped  # 크래시·재부팅 후 자동 재기동 (24h 적립봇)
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/api/health').status==200 else 1)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  frontend:
    build: ./frontend
    ports:
      - "127.0.0.1:8080:80"  # 서버 자신만 접근(외부 노출 X). SSH터널/Tailscale로 접속
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

volumes:
  bot_data:
